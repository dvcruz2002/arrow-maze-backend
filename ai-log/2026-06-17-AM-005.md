# AI Log — AM-005 — Implement Identity application services

**Date:** 2026-06-17
**Ticket:** MAZ-76 (AM-005)
**Branch:** feat/identity-application-AM-005

## What was generated

- `src/application/identity/ports/UserRepository.ts`
- `src/application/identity/ports/PasswordHasher.ts`
- `src/application/identity/ports/TokenService.ts`
- `src/application/identity/use-cases/RegisterUserUseCase.ts`
- `src/application/identity/use-cases/LoginUseCase.ts`

## AI decisions and rationale

- `PasswordHasher` and `TokenService` defined as ports so infrastructure (bcrypt, JWT) stays outside the application layer.
- `LoginUseCase` catches domain `InvalidArgumentError` from value object validation and re-throws as `UnauthorizedError("Invalid credentials")` to avoid leaking whether the account exists.
- Reused existing `ConflictError`, `UnauthorizedError`, `ForbiddenError` from `shared/errors` — no new error classes created.
- `TokenPayload` carries only `userId` and `role` — the use case does not pass the full `User` aggregate to the token service.

## Human review notes

- Verify that the order of checks in `LoginUseCase` (email format → user lookup → password verify → status check) matches the team's security expectations.
- Confirm `existsByEmail` and `existsByUsername` are needed as separate queries vs. a single `findByEmail` + manual check in `RegisterUserUseCase`.
